<div class="p-5">
  <h3>🤐 New Review :</h3>
  <br>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie, as: :select, prompt: "Choose a Movie" %>
    <%= f.input :comment %>
    <br>
    <%= f.button :submit, class:"btn btn-outline-secondary" %>
  <% end %>
</div>
