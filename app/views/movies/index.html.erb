<div class="container p-5">
    <h1>👀 All Movies</h1>
</div>
<div class="container">
<% @movies.each do |movie|%>
    <div class="container">
        <div class="card-product">
            <img src="<%= movie["poster_url"]%>"/>
            <div class="card-product-infos">
              <h2><%=movie["title"]%></h2>
              <p><%=movie["overview"]%><p>
              <p class="rating">Viewer ratings : <strong><%=movie["rating"]%>/10<strong><p>
            </div>
        </div>
    </div>
    <% end %>
</div>
